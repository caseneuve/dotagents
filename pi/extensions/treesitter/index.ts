import type { ExtensionAPI } from "@mariozechner/pi-coding-agent";

/**
 * Tree-sitter extension scaffold.
 *
 * 0006.1 is intentionally design-first. Parser integration and tool
 * registration are implemented in 0006.2 and 0006.3.
 */
export default function (_pi: ExtensionAPI) {
  // Intentionally no runtime behavior yet.
}
