repos:
  - repo: local
    hooks:
      - id: strip-ai-coauthor
        name: Strip AI Co-authored-by
        language: system
        entry: perl -i -ne 'print unless /^Co-[Aa]uthored-[Bb]y:.*noreply/'
        stages: [commit-msg]
        always_run: true
