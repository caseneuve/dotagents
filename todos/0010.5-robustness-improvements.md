---
title: "Robustness improvements: TTL cap, logging, race condition docs"
status: done
priority: low
type: chore
labels: [code-excellence]
created: 2026-04-14
parent: "0010"
blocked-by: []
blocks: []
---

## Context

Grab-bag of robustness improvements identified in the code design review.
Independent of the FCIS refactor — can land anytime.

## Acceptance Criteria
- [x] `ownMessageIds` has TTL or max-size cap (e.g. keep last 1000 IDs)
- [x] `readChannelFile` logs on parse error instead of silent swallow
- [x] `execArgs` logs on failure instead of returning empty string silently
- [x] Document race condition on file writes (read-mutate-write not atomic)
- [x] Channel file pruning strategy documented (messages accumulate forever)

## Affected Files
- `pi/extensions/agent-channel/index.ts` (or post-split modules)

## Notes
- Race condition is acceptable for local dev but should be documented
- File pruning could be a `/channel-prune` command or automatic TTL
