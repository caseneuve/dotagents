---
title: "Extract pure core into core.ts + dedup filtering + fix require"
status: open
priority: high
type: refactor
labels: [code-excellence, FCIS]
created: 2026-04-14
parent: "0010"
blocked-by: []
blocks: [0010.2, 0010.4]
---

## Context

The agent-channel extension has pure functions mixed into the monolith. Both backends
duplicate identical filter chains in their `read()` methods. One `require()` call is
inconsistent with the ESM import style.

## Acceptance Criteria
- [ ] Create `core.ts` with extracted pure functions:
  - `ackMessages()` — fully pure (return new ChannelFile, no mutation)
  - `shouldTriggerTurn()` — predicate on ChannelMessage
  - `channelPath()` — string → string
  - `filterMessages()` — new, shared filter logic
- [ ] Both `CmuxBackend.read()` and `FileOnlyBackend.read()` call `filterMessages()`
- [ ] `require("node:child_process")` replaced with top-level `import`
- [ ] No behavior changes — all existing functionality preserved

## Affected Files
- `pi/darwin/extensions/agent-channel/index.ts` — extract from here
- `pi/darwin/extensions/agent-channel/core.ts` — new file

## Notes
- `ackMessages` currently mutates in-place. Make it return `{ file: ChannelFile, ackedCount: number }`
- `filterMessages(msgs, opts)` handles since/unacked/type filtering
