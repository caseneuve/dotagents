---
title: move subscription usage widget to ~/git/pi/pi-usage
status: done
priority: medium
type: chore
labels: [pi, packaging]
created: 2026-07-10
parent: 0027
blocked-by: [0027.5]
blocks: [0027.9]
---

## Context

Move the existing subscription usage overlay into a focused public package created from the
standard Pi package Copier template. Preserve its current provider/account behavior and limitations;
do not improve provider detection, credentials, or failure handling during extraction. The Bedrock
empty-response mitigation is intentionally retired because the underlying issue is believed fixed
upstream.

## Acceptance Criteria

- [x] Create `github.com/caseneuve/pi-usage` with local checkout `~/git/pi/pi-usage`, generated from `~/git/copier/template-pi-package/`.
- [x] Before extraction, audit couplings to other dotagents resources and track every unresolved coupling as a release-blocking todo in the destination repository.
- [x] Move `usage.ts` as the package's single explicit extension resource with a stable identity.
- [x] Do not move `bedrock-empty-response-mitigation.ts`.
- [x] Preserve the current `/usage` command, provider matching, credential sources, refresh behavior, error behavior, reset-credit display, and configuration.
- [x] Create destination todo `0001` for the Prolite usage-window labeling bug and treat it as a blocker for the first release: derive labels from reported window duration rather than assuming `primary_window` is five-hour and `secondary_window` is weekly.
- [x] Move existing tests/documentation and make tests pass with documented commands.
- [x] Local-path and immutable-ref Git installs work with supported `@earendil-works/*` APIs.
- [x] Document current provider/account assumptions without expanding capability.

## Affected Files

- `pi/extensions/usage.ts`
- `test/pi/usage.test.ts`
- `todos/0068-codex-usage-resets.md` — completed source provenance for the merged reset-credit behavior.
- New `~/git/pi/pi-usage` repository.
