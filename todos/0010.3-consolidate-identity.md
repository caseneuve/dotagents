---
title: "Consolidate identity management into AgentIdentity"
status: open
priority: medium
type: refactor
labels: [code-excellence, FCIS]
created: 2026-04-14
parent: "0010"
blocked-by: [0010.1]
blocks: [0010.4]
---

## Context

Agent identity is spread across `agentId`, `agentLabel`, `agentName()`, and
`CMUX_AGENT_NAME` env var with a priority chain. Works but hard to reason about.

## Acceptance Criteria
- [ ] Single `AgentIdentity` structure: `{ id: string, label?: string, env?: string }`
- [ ] `resolve()` method with clear priority: env > label > id
- [ ] Persist/restore as one unit via `appendEntry`
- [ ] `agentName()` replaced with `identity.resolve()`

## Affected Files
- `pi/darwin/extensions/agent-channel/identity.ts` — new file
- `pi/darwin/extensions/agent-channel/index.ts` — use new structure

## Notes
- Priority chain: CMUX_AGENT_NAME env > user-set label > session-generated id
- Must remain compatible with `/agent-name` command and system prompt injection
