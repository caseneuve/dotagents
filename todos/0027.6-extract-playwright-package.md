---
title: move Playwright extension to ~/git/pi/pi-playwright
status: open
priority: high
type: feature
labels: [pi, packaging]
created: 2026-07-10
parent: 0027
blocked-by: [0027.5]
blocks: [0027.9, 0052]
---

## Context

Move the existing Playwright extension into a public, independently installable package. Preserve
its current dependency versions, manual browser-install workflow, tools, policy, and tests. Do not
turn extraction into browser lifecycle or CI redesign.

## Acceptance Criteria

- [ ] Create `github.com/caseneuve/pi-playwright` with local checkout `~/git/pi/pi-playwright`.
- [ ] Move the complete extension, current package/lock metadata, tests, policy UI/storage, and documentation.
- [ ] Preserve all current tool names/behavior, restrictive allowlist defaults, screenshot rules, and shutdown behavior.
- [ ] Document the current runtime dependency and Chromium/browser installation steps exactly as required today.
- [ ] Local-path and immutable-ref Git installs register the current tool set with supported `@earendil-works/*` APIs.
- [ ] Existing tests pass with documented commands.
- [ ] Browser-install automation, version-management improvements, and broader CI belong to follow-up `0052`.

## Affected Files

- `pi/extensions/playwright/*`
- New `~/git/pi/pi-playwright` repository.

## E2E Spec

GIVEN the package and browser dependencies installed using the documented existing workflow
WHEN Pi uses the current browser tools against an allowed URL
THEN behavior and policy enforcement match the dotagents-loaded extension.
